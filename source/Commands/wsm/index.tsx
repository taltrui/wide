import { Text } from "ink";
import React, { FC } from "react";
import { WsmType } from "../../types";

const Wsm: FC<WsmType> = ({}) => <Text bold>Emuladores disponibles:</Text>;

export default Wsm;
